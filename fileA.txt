Hello Everyone!!!

1. create a folder for practicing git
2. open visual studio in that folder and open the terminal ( find it in the view menu) 
3. runt git config --global user.name "Your Name"
4. git config --global user.email "your_email@whatever.com"
5 run git init in the terminal
6.add a file called fileA.txt and add text
7. add the file to the index by git add fileA.txt
8. Save the file by running `git commit -m 'first commit'
9. make a change to the file and save it
10. run `git status` to see that git sees changes
11. run `git add fileA` to add the changes to the index
12. run ` git commit -m 'made changes'
13. run ` git status` to check that there are no more changes to save.



whatever text


1. modify fileA.txt
2. add to the index (staged area) by running `git add .`
3. run git status
4. remove from the staging are by running `git reset`
5. run `git status`



1. modify fileA.txt
2. run `git log` to get the latest commit hash/id
3. run the dangerous command `git reset --hard <your commit hash/id>


1. create/sign in to GitHub.com
2. Click the plus button to create a new repo and give it a name
3. copy the https link to repo
4. go to your terminal and add the remote by running `git add origin <your_url_to_your_repo>`
5. push to GitHub your changes by running `git push origin master -u`


1. create a new branch called newFeature by running ` git checkout -b newFeature'
2. add some text to the fileA.txt
3. commit the changes by running git commit -am 'new feature'`
4. go to the master branch by running git checkout master
5. create a bugFix branch called bugFix by running `git checkout -b 'bugFix'
6. create a file called fileB.txt and run `git add .` and then `git commit -m 'bug fix'
7. push your bugFix branch by running `git push origin bugFix -u`